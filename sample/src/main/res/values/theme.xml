<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="CustomActionBarTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/button_selected</item>
        <item name="android:titleTextStyle">@style/TitleTextStyle</item>
        <item name="android:homeAsUpIndicator">@drawable/back</item>
        <!-- Support library compatibility -->
        <item name="background">@color/button_selected</item>
        <item name="titleTextStyle">@style/TitleTextStyle</item>
        <item name="homeAsUpIndicator">@drawable/back</item>
    </style>

    <style name="TitleTextStyle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">@dimen/actionbar_textsize</item>

    </style>
</resources>